services:
  # PostgreSQL - Banco de dados para Evolution API
  postgres:
    container_name: evolution_postgres
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - evolution_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis - Cache para Evolution API
  redis:
    container_name: evolution_redis
    image: redis:alpine
    restart: always
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - evolution_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Evolution API v2.3.7+ (com suporte a remoteJidAlt para resolver LIDs)
  evolution-api:
    container_name: evolution_api
    image: evoapicloud/evolution-api:latest
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env.evolution
    volumes:
      - evolution_instances:/evolution/instances
    networks:
      - evolution_network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  # LifeOS Agent - Agente principal com SQLite
  lifeos-agent:
    container_name: lifeos_agent
    build:
      context: .
      dockerfile: Dockerfile.agent
    restart: always
    ports:
      - "8000:8000" # ADK API Server
      - "3002:3002" # Webhook
    environment:
      - ADK_API_URL=http://localhost:8000
      - WEBHOOK_PORT=3002
      - DB_PATH=/data/lifeos.db
    env_file:
      - .env
    volumes:
      # Monta o banco local no container
      - ./life_os_agent/database:/data
    networks:
      - evolution_network
    depends_on:
      - evolution-api

volumes:
  postgres_data:
  redis_data:
  evolution_instances:

networks:
  evolution_network:
    driver: bridge
